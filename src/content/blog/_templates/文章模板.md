<%*
const date = tp.date.now("YYYY-MM-DD");
const folder = tp.file.folder(false) || "";
const parts = folder.replace(/\\/g, "/").split("/").filter(Boolean);
const known = ["算法题", "八股", "学习笔记", "个人记录", "声乐", "提升自己", "待添加"];
const category = parts.reverse().find((p) => known.includes(p)) || "";
tR = `---
pubDate: ${date}
category: ${category}
tags: []
---

`;
%>
